/* eslint-disable -- generated by scripts/generate-stories.mjs */
import type { Meta, StoryObj } from '@storybook/react';
import { expect, within } from '@storybook/test';
import { getExamplesForSlug } from '@m3ui/examples';

const examples = getExamplesForSlug("navigation-bar");

const meta = {
  title: 'Navigation/Navigation Bar',
  tags: ['autodocs'],
  parameters: {
    docs: {
      description: {
        component: "Bottom navigation bar with active indicator pill and badges",
      },
    },
  },
} satisfies Meta;

export default meta;

type Story = StoryObj<typeof meta>;

export const Destinations: Story = {
  name: "Destinations",
  render: () => {
    const Example = examples[0]!.Component;
    return <Example />;
  },
};

export const SmokeTest: Story = {
  play: async ({ canvasElement }) => {
    const canvas = within(canvasElement);
    await expect(canvasElement.firstChild).toBeTruthy();
  },
};
