/* eslint-disable -- generated by scripts/generate-stories.mjs */
import type { Meta, StoryObj } from '@storybook/react';
import { expect, within } from '@storybook/test';
import { getExamplesForSlug } from '@m3ui/examples';

const examples = getExamplesForSlug("adaptive-navigation");

const meta = {
  title: 'Navigation/Adaptive Navigation',
  tags: ['autodocs'],
  parameters: {
    docs: {
      description: {
        component: "Auto-switching navigation bar, rail, and drawer",
      },
    },
  },
} satisfies Meta;

export default meta;

type Story = StoryObj<typeof meta>;

export const Navigationbarmode: Story = {
  name: "Navigation bar mode",
  render: () => {
    const Example = examples[0]!.Component;
    return <Example />;
  },
};

export const Storybookdemo: Story = {
  name: "Storybook demo",
  render: () => {
    const Example = examples[1]!.Component;
    return <Example />;
  },
};

export const SmokeTest: Story = {
  play: async ({ canvasElement }) => {
    const canvas = within(canvasElement);
    await expect(canvasElement.firstChild).toBeTruthy();
  },
};
