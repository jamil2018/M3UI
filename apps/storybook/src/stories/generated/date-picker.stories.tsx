/* eslint-disable -- generated by scripts/generate-stories.mjs */
import type { Meta, StoryObj } from '@storybook/react';
import { expect, within } from '@storybook/test';
import { getExamplesForSlug } from '@m3ui/examples';

const examples = getExamplesForSlug("date-picker");

const meta = {
  title: 'Pickers/Date Picker',
  tags: ['autodocs'],
  parameters: {
    docs: {
      description: {
        component: "Docked, modal, and range date pickers with calendar engine",
      },
    },
  },
} satisfies Meta;

export default meta;

type Story = StoryObj<typeof meta>;

export const Variants: Story = {
  name: "Variants",
  render: () => {
    const Example = examples[0]!.Component;
    return <Example />;
  },
};

export const Docked: Story = {
  name: "Docked",
  render: () => {
    const Example = examples[1]!.Component;
    return <Example />;
  },
};

export const SmokeTest: Story = {
  play: async ({ canvasElement }) => {
    const canvas = within(canvasElement);
    await expect(canvasElement.firstChild).toBeTruthy();
  },
};
