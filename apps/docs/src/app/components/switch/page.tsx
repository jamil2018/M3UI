'use client';

import { Switch } from '@m3ui/react';
import { componentPage } from '@/components/component-page';

export default componentPage('switch', 'Switch', <Switch label="Notifications" defaultChecked />);
