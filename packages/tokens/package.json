{
  "name": "@m3ui/tokens",
  "version": "0.0.0",
  "description": "Material Design 3 Expressive design tokens — CSS custom properties and TypeScript types",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./tokens.css": "./src/generated/tokens.css",
    "./theme.css": "./src/generated/theme.css"
  },
  "files": [
    "dist",
    "src/spec",
    "src/generated"
  ],
  "scripts": {
    "codegen": "tsx scripts/codegen.ts",
    "build": "pnpm codegen && tsdown && node scripts/copy-css.mjs",
    "dev": "pnpm build && tsdown --watch",
    "typecheck": "tsc --noEmit && tsc --noEmit -p scripts/tsconfig.json",
    "lint": "eslint \"src/**/*.ts\" \"scripts/**/*.ts\" --ignore-pattern \"**/*.test.ts\" --ignore-pattern \"**/*.spec.ts\"",
    "test": "vitest run --config vitest.config.ts"
  },
  "devDependencies": {
    "tsdown": "^0.9.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
